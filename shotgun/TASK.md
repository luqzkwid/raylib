#### Here is many cool tasks for you to do in this project.
#####  1: Make a new Special Bullet, that have a bigger Radius (so it will eliminates more enemies).
- hint:
-    The way i would do this is very simple, i would create a bool variable **special_shoot** and verify it, while my game is running, so:
        '''
        if(special_shoot) {
            - Create the special shoot and use it in the function that verify the shoot...
        }
        '''
        so then, to activate this special shoot just verify if the key K is pressed and "turn the variable on" (just change it to true if it was false, or change it to false if it was true.).
##### 2: Introduce a new type of model for the gun or the character.
##### 3: Maybe add special enemies to appers every 3 rounds (every 3 times that Key R is pressed).
